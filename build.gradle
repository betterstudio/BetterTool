plugins {
    id 'java-library'
    id 'idea'
    id 'maven'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

compileJava.options.encoding = 'UTF-8'

group 'fr.better'
version 'v1.17-alpha.3'

dependencies {
    compileOnly fileTree(dir:'libs',includes:["*.jar"])
}